spring:
  application:
    name: service-feign

server:
  port: 8765

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/

#打开Feign是自带断路器,默认关闭
feign:
 hystrix:
  enabled: true